import { HavidijFormatPipe } from './havidij-format.pipe';

describe('HavidijFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new HavidijFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
